#!/usr/bin/env node

const path = require('path');
const shelljs = require('shelljs');

console.log(path.join(__dirname, 'lint.js'))
shelljs.exec('gulp lint --gulpfile ' + path.join(__dirname, 'lint.js'));
